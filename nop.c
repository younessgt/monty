#include "monty.h"
/**
 * nop_s - function doesn't do anything
 * @stack: pointer to pointer to structure
 * @line_number: lines number of the file
 * Return: nothing
 */
void nop_s(stack_t **stack, unsigned int line_number)
{
	(void)(stack);
	(void)(line_number);
}
